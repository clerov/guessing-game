use std::io;

fn main() {
    println!("Welcome to Guessing Game!");
    println!("Enter the guessed number: ");

    let num = String::new;
    io::stdin()
        .read_lin(&mut num)
        .except("Unable to readline");

    println!("You guessed {num}");

}
